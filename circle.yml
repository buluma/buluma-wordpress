machine:
  environment:
    SFTP_USERNAME: bulumame
    SFTP_PASSWORD: SFTP_PASSWORD
    SFTP_HOSTNAME: SFTP_HOSTNAME
    THEME_NAME: pingraphy
test:
  override:
    - /bin/true
#deployment:
#  production:
#    branch: master
#    commands:  
#      - ./deploy.sh
