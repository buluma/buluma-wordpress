machine:
  environment:
    SFTP_USERNAME: your_username_here
    SFTP_PASSWORD: your_secret_password
    SFTP_HOSTNAME: sftp.pressftp.com
    THEME_NAME: your_theme_name
test:
  override:
    - /bin/true
deployment:
  production:
    branch: master
    commands:  
      - ./deploy.sh